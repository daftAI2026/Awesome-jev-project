# Cloudflare Workers — Vite SPA (static assets)
# Workers Builds (Git): push to main → npm run build → npx wrangler deploy

name = "awesome-jev"
compatibility_date = "2026-09-18"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
