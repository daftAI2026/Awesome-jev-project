# Collector-only TypeSafe key. Copy to `.env.local` (gitignored) and fill in.
# Mint a key at https://console.typesafe.ai/settings/keys
# Do not paste the real key in chat, commit it, or expose it to the Vite client.
TYPESAFE_API_KEY=
